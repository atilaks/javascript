// Formas de declarar un array
var lista1 = ["coche", 54, "bici"];
var lista2 = new Array("coche", 54, "bici");

// Acceso a índices
pos1 = lista1[0]

// Propiedad length: longitud de una lista
lista1.length;

// Método push: agregar elemento al final de una lista
lista1.push("balón")

// Método unshift: agregar elemento al principio de una lista
lista1.unshift(6)

// Método pop: elimina el último elemento de la lista
lista1.pop()

// Método shift: elimina el primer elemento de la lista
lista1.shift()
